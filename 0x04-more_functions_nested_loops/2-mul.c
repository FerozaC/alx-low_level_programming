#include "main.h"
/**
 * mul-multiply two int
 * @a: The first to multiply
 * @b: The second to multipy with first number
 * Return: addition of multiplication
 */
int mul(int a, int b)
{
	return (a*b);
}
